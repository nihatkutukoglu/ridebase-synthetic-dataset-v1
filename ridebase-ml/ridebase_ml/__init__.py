"""RideBase ML — production inference packages.

Currently exposes ``ridebase_ml.v2`` (V2 survival ensemble). Training lives in the
notebooks; this package is inference-only.
"""
__version__ = "0.2.0"
